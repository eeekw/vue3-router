import { MatchedRouteLocation } from '../types'

// eslint-disable-next-line import/prefer-default-export
export const ROUTE_LOCATION_INIT: MatchedRouteLocation = {
  path: '',
  // hash: '',
  // query: '',
  matched: []
}